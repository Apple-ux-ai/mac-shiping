# 项目问题与决策记录 (ISSUES.md)

## 待解决问题

| 编号 | 问题描述 | 影响范围 | 可选方案 | 推荐方案 | 需要决策的内容 | 状态 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| ISS-001 | 面包屑导航中的中间层级是否需要可点击？ | 导航体验 | 1. 仅显示文本；2. 添加跳转链接到对应分类页 | 方案 2 (如果存在分类页) | 是否有“AVI 转换器”分类页？ | 已通过导航逻辑优化处理 |
| ISS-002 | `UnifiedToolHeader` 的布局在 padded 容器中是否合适？ | UI 视觉效果 | 1. 保持现状；2. 将 header 移出 padded 容器以实现全宽效果 | 方案 2 | Header 是否应该全宽显示？ | 已完成布局调整 |

## 已决策记录

| 编号 | 决策描述 | 决策理由 | 影响范围 |
| :--- | :--- | :--- | :--- |
| DEC-001 | 使用 `UnifiedToolHeader` 替换原有的“返回”按钮 | 符合图 2 的导航要求，提升一致性 | 所有转换器页面 |
| DEC-002 | `SettingSlider` 增加 `valueDisplay` 属性 | 允许自定义显示复杂参数（如“每 X 帧提取一页”） | 所有使用滑块的参数配置组件 |
| DEC-003 | 统一参数标签为“质量”、“帧率”、“提取间隔” | 提升全站术语一致性，降低用户理解成本 | 所有转换器页面及配置组件 |
